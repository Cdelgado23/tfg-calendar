# TFG - CARLOS DELGADO GUIBERTEAU 

Build With React 

CI with Github Actions

Launch Docker container: 
- docker run -it --rm -v ${PWD}:/app -v /app/node_modules -p 3001:3000 -e CHOKIDAR_USEPOLLING=true tfg-calendar:dev